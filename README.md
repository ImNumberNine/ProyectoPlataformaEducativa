Adjust the appsettings.json to your database server to get the full functionality of the project.

Run the following commands to get the database:

dotnet ef migrations add LSMVC
dotnet ef database update

